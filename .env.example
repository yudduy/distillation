# Local public evaluation only. Copy to .env and chmod 600 .env.
# Add a new dedicated capped key locally; never paste it into chat or commit it.
OPENROUTER_API_KEY=
# Keep disabled until the exact experiment and USD cap are approved.
DISTILL_LIVE=0
DISTILL_MAX_SPEND_USD=
# The current evaluator reads exported environment variables, not dotenv files.
# From a trusted local shell: set -a; source .env; set +a
# Ranked dataset delivery and operator secrets belong in the protected workflow.
